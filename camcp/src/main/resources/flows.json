{
  "tools": {
    "send-weather-events": {
      "connectors": ["kafka-weather", "kafka-weather2", "dont-use-me"]
    },
    "send-random-events": {
      "connectors": ["kafka-random"]
    }
  },
  "connectors": {
    "kafka-weather": {
      "priority": 1,
      "name": "kafka-weather",
      "endpoint": "kafka:weather-events",
      "params": { "brokers": "localhost:9092" }
    },
    "kafka-weather2": {
      "priority": 2,
      "name": "kafka-weather2",
      "endpoint": "kafka:weather-archive",
      "params": { "brokers": "localhost:9092" }
    },
    "dont-use-me": {
      "priority": 3,
      "name": "dont-use-me",
      "endpoint": "kafka:dont-use-me",
      "params": { "brokers": "localhost:9092" }
    },
    "kafka-random": {
      "priority": 1,
      "name": "kafka-random",
      "endpoint": "kafka:random-events",
      "params": { "brokers": "localhost:9092" }
    }
  }
}